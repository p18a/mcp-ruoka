# Generate with: openssl rand -hex 32
MCP_AUTH_TOKEN=

# OAuth client credentials (for claude.ai and other MCP clients)
# Generate with: openssl rand -hex 32
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
PORT=3001
HEADLESS=true
BROWSER_DATA_DIR=
